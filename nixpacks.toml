providers = ["node", "php"]


[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build:all"]

[start]
# We let the default PHP provider handle the start command (Nginx + PHP-FPM)
